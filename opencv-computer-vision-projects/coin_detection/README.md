# Coin Detection and Counting

Detects circular coins in an image using OpenCV's Hough Circle Transform.

## Run

```bash
python coin_detection.py --input ../images/coins.jpg
```

The default output is `coin_result.jpg`.
