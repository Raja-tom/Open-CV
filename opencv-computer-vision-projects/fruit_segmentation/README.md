# Fruit Image Segmentation

Segments red fruits from an image using HSV thresholding and morphological operations.

## Run

```bash
python fruit_segmentation.py --input ../images/fruits.jpg
```

The default output is `fruit_segmented.jpg`.

To segment another fruit color, adjust the HSV ranges in the Python file.
